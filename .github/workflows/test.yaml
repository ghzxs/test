name: Use WARP in Actions

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Install WARP
        run: |
          bash <(curl -fsSL https://gitlab.com/fscarmen/warp/-/raw/main/menu.sh) d
      - name: Check IP
        run: curl ifconfig.me