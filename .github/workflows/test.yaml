name: Use WARP in Actions

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Install WARP
        run: |
          wget -N https://gitlab.com/fscarmen/warp/-/raw/main/menu.sh && sudo bash menu.sh d
      - name: Check IP
        run: curl ifconfig.me